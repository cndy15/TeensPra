const paragraph = 'JavaScript is a programming languange or , in other word, a means by a computer is instructed to do things';

const paragraphLen = paragraph.length;

const paragraphLowercase = paragraph.toLowerCase();

console.log(paragraph === paragraphLowercase);
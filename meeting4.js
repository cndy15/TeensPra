//TASK1
const monthlyShoppingList = ['Rice', 'Eggs', 'Milk', 'Lowfat Yogurt', 'Bread', 'Wheat Bread'];

const importantToBuy = monthlyShoppingList[5];
console.log(importantToBuy);

monthlyShoppingList[3] = 'Soy Milk';
console.log(monthlyShoppingList);

console.log(monthlyShoppingList.length);

// TASK2
const holidayPlans = ['Paris', 'Tokyo', 'Sidney', 'New York'];
console.log(holidayPlans);